# ── Python ───────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/

# ── Virtual environment ──────────────────────────────
venv/
.venv/

# ── Environment & secrets ────────────────────────────
.env
.env.local
.env.*.local

# ── Runtime logs ─────────────────────────────────────
*.log
serverlog.txt
serverlog.txt.*

# ── Test / evaluation artifacts ──────────────────────
eval_*
eval_mock_output.txt
eval_report_*.json
.deepeval/
.pytest_cache/
.coverage
coverage/
htmlcov/

# ── Ad-hoc scripts never meant for repo ──────────────
test_propflow_backend.py
memory_saver_source.py

# Local diagnostic / one-off scripts (never commit)
scripts/backfill_deterministic_terms.py
scripts/clean_property_addresses.py
scripts/diag_payment_freq.py
scripts/inspect_active_thread.py
scripts/inspect_fixture_global.py
scripts/inspect_fixture_phase.py
scripts/inspect_maitama_budget.py
scripts/patch_agreement_terms.py
scripts/verify_validator_live.py
verify_out.txt

# ── Data files / generated outputs ───────────────────
*.csv
*.xlsx
*.parquet

# ── IDEs ─────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# ── Node / frontend build artifacts ──────────────────
node_modules/
.next/

# ── Dangerous database cleanup scripts ───────────────
scripts/clean_database_for_demo.py
scripts/reset_test_data.py

# ── SQL & Database Files (kept private in /database) ─
*.sql
docs/
app/db_migrations/
dev_tests/
migrations/

# Mem0 local vector store — runtime data, never commit. Relocated to
# %LOCALAPPDATA%\Nulo\mem0 so uvicorn --reload doesn't watch it.
.mem0_store/
